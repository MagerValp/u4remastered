MEMORY {
    LOADADDR:       start =  $800,  size =     2,   file = %O;
    SUBS:           start =  $800,  size = $1800,   file = %O;
}
SEGMENTS {
    LOADADDR:       load = LOADADDR,        type = ro;
    SUBS:           load = SUBS,            type = rw;
}
