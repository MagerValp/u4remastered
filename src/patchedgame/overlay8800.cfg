MEMORY {
    LOADADDR:       start = $8800,  size =     2,   file = %O;
    OVERLAY:        start = $8800,  size = $1400,   file = %O;
}
SEGMENTS {
    LOADADDR:       load = LOADADDR,        type = ro;
    OVERLAY:        load = OVERLAY,         type = rw;
}
