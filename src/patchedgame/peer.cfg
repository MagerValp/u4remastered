MEMORY {
    LOADADDR:       start = $9000,  size =     2,   file = %O;
    PEER:           start = $9000,  size =  $500,   file = %O;
}
SEGMENTS {
    LOADADDR:       load = LOADADDR,        type = ro;
    PEER:           load = PEER,            type = rw,  align = $100;
}
