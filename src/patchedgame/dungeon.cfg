MEMORY {
    LOADADDR:       start = $8c00,  size =     2,   file = %O;
    DUNGEON:        start = $8c00,  size = $1000,   file = %O;
}
SEGMENTS {
    LOADADDR:       load = LOADADDR,        type = ro;
    DUNGEON:        load = DUNGEON,         type = rw,  align = $100;
}
