MEMORY {
    LOADADDR:       start = $8800,  size =     2,   file = %O;
    TALK:           start = $8800,  size =  $800,   file = %O;
    DIALOG:         start = $ad00,  size =  $100,   file = "";
}
SEGMENTS {
    LOADADDR:       load = LOADADDR,        type = ro;
    TALK:           load = TALK,            type = rw;
    DIALOG:         load = DIALOG,          type = rw;
}
