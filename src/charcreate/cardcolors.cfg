MEMORY {
    LOADADDR:       start = $9800,  size =     2,               file = %O;
    CARDCOLORS:     start = $9800,  size =  $600,               file = %O;
}
SEGMENTS {
    LOADADDR:       load = LOADADDR,        type = ro;
    CARDCOLORS:     load = CARDCOLORS,      type = rw;
}
